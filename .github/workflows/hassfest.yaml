name: Hassfest

on:
  push:
    paths:
      - "custom_components/**"
  pull_request:
    paths:
      - "custom_components/**"
  schedule:
    - cron: "0 0 * * *"

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: home-assistant/actions/hassfest@master
